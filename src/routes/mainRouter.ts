// import { Router } from "express";
// import mainPageController from "../controllers/mainPageController.js";

// const mainRouter = Router();

// mainRouter.get("/", (req, res) => mainPageController.getMainPage(req, res));

// export default mainRouter;
